#!/usr/bin/python3
def square_matrix_simple(matrix=[])
    new_matrix = []
    for item in matrix:
        square_list = list(map(lambda x: x*x, item))
        new_matrix.append(square_list)
    return(new_matrix)
