#!/usr/bin/python3
'''This module contains a function that checks for builtin classes'''


def add_attribute(obj, attr_name, attr_value):
    setattr(obj, attr_name, attr_value)

if __name__ == "__main__":
    add_attribute(obj, attr_name, attr_value)
